"""Asset loading helpers for the Star Nexus lab environment.

Responsibilities to implement later:

* Resolve relative paths for brain/world JSON assets bundled with the repo.
* Provide convenience wrappers around :mod:`json` and :mod:`pydantic` validation.
* Offer caching / memoisation so repeated notebook runs remain fast.
"""

# TODO: Build asset discovery + caching helpers.
